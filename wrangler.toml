name = "ltr"
main = "src/worker.js"
compatibility_date = "2026-07-13"

[assets]
directory = "./dist"
binding = "ASSETS"

# TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID are set as encrypted secrets via
# `wrangler secret put <NAME>`, not committed here.
