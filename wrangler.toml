name = "ltr-pages"
pages_build_output_dir = "dist"

# TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID are set as encrypted secrets via
# `wrangler pages secret put <NAME> --project-name=ltr`, not committed here.
